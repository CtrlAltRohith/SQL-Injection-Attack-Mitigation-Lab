# SQL-Injection-Attack-Mitigation-Lab
Using OWASP Juice Shop as an intentionally vulnerable target application, three categories of SQL Injection were successfully demonstrated. Each finding was documented with the exact payload used, the application's response, and the root cause in the underlying query logic. Then secure code equivalents were implemented
